---
name: task-feature
description: SUPERSEDED — use task-plan (for planning) and task-execute (for implementation) instead.
---

This skill has been replaced by the `task-plan` + `task-execute` pair.

- For planning a new feature set: invoke `task-plan`
- For implementing an approved milestone: invoke `task-execute`
